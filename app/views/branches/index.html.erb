<div style="padding:20px 0 30px">
  <h1>Lista de sucursales</h1>
  <%= @pr %>
</div>
<% if @branches.empty?  %>
  <h3>Agrega una sucursal </h3>
<% else %>
  <table class="table table-striped">
  <thead>
    <tr>

      <th>Nombre</th>
      <th>Ubicación</th>
      <th>Proveedor</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @branches.each do |branch| %>
    <tr>
      <td><%= branch.name %></td>
      <td><%= branch.location %></td>
      <% if branch.provider == true %>
        <td>si</td>
        <td></td>
      <% else %>
        <td>no</td>
        <td><%= link_to 'Show', branch, :class=>"btn btn-primary" %></td>
      <% end %>
      <td><%= link_to 'Edit', edit_branch_path(branch), :class=>"btn btn-primary" %></td>
      <td><%= link_to 'Destroy', branch, confirm: 'Are you sure?', method: :delete, :class=>"btn btn-danger" %></td>
    </tr>
  </tbody>
  <% end %>
  </table>
<% end  %>
<br />

<%= link_to 'Nueva Sucursal', new_branch_path, :class => "btn btn-large btn-primary" %>
